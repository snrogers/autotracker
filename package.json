{
  "name": "autotracker-cli",
  "version": "0.1.0",
  "description": "CLI version of an endless randomised algorithmic Chiptune composition by David Whiting (Vitling/Demoscene Time Machine)",
  "type": "module",
  "scripts": {
    "start": "bun src/cli.ts",
    "typecheck": "tsc --noEmit",
    "build:amd64": "docker build -t autotracker-builder . && docker create --name temp-container autotracker-builder && docker cp temp-container:/app/autotracker-amd64 ./autotracker-amd64 && docker rm temp-container"
  },
  "author": "David Whiting",
  "license": "CC-BY-4.0",
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/bun": "latest"
  },
  "private": true,
  "dependencies": {
    "node-web-audio-api": "^1.0.4"
  }
}